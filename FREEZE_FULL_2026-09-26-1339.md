# FULL FREEZE 2026-09-26-1339 — complete tradingai.in source (owner-ordered)

- Date: Sat Sep 26 13:39 IST 2026
- Tarball: /home/ubuntu/backups/tradingai-full-2026-09-26-1339.tar.gz + .sha256
  (`4bdf69a1...94837e` — verify with `sha256sum -c`)
- Contains: full /opt/tradingai tree (code, frontend incl. frozen sets, tests,
  scripts, config, docs, database/tradingai.db), nginx site config, apache
  tradingai.in conf, tradingai systemd units. Excludes: __pycache__,
  .pytest_cache, *.pyc, rotated/compressed logs.
- Git HEAD: cb5ee1c (working tree dirty vs HEAD — approved changes; see
  VM_DEVELOPMENT_HISTORY.md).
- Permissions (READ-ONLY until owner authorizes):
  - code/frontend/config/tests/scripts/docs: dirs 555, files 444.
  - runtime-writable (services REQUIRE these): database/, logs/, data/,
    research/, frontend/ dirs 775; database/tradingai.db,
    frontend/paper.html, files under logs/, data/generated/, research/ 664.
  - two root-owned log files (gunicorn-err.log.2.gz, gunicorn-out.log) keep
    root ownership (pre-existing).
- Verified post-lockdown: API restarted + serving, decision endpoint reads OK,
  DB write probe OK (rolled back, no residue), collector oneshot exit 0.
- Restore: tar -xzf <tarball> -C / (as root; re-apply perms if needed).
- To unfreeze: chmod -R u+w /opt/tradingai ONLY on owner order.
